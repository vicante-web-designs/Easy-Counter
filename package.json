{
    "name": "easycounter",
    "private": true,
    "workspaces": [
        "app/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=app/client\" \"npm run dev --workspace=app/server\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "nodemon": "^3.1.14",
        "ts-node": "^10.9.2",
        "typescript": "^6.0.2"
    },
    "dependencies": {
        "axios": "^1.15.0"
    }
}
